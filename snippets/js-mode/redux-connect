# -*- mode: snippet -*-
# name: redux-connect
# key: rconnect
# --
const $1 = connect(
    ${4:function mapStateToProps(state, ownProps) {
        return ${5:{$6}};
    }},
    ${7:function mapDispatchToProps(dispatch, ownProps) {
        return ${8:{$9}};
    }}
)($10);

export default $1;