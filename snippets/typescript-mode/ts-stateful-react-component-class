# -*- mode: snippet -*-
# name: ts-stateful-react-component-class
# key: r:C:s:t
# --
import * as React from "react";

export interface ${1:`(f-base (buffer-file-name))`}Props {$2
}

interface $1State {$3
}

export class $1 extends React.Component<$1Props,$1State> {

    state = {$4
    }

    render() {
        return (
            $5
        );
    }
}
