# -*- mode: snippet -*-
# name: mst-model
# key: mst:model
# --
import { types as t } from "mobx-state-tree";

export const $1 = t
    .model("$1", ${6:{$11}})${16:
    .views(self => ({$21}))}${26:
    .actions(self => ({$31}))}${36:
    .actions(self => {
        $41return {$46};
    })}

export type I$1 = typeof $1.Type;
export type I$1Snapshot = typeof $1.SnapshotType;
